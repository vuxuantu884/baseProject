declare type PluginName = 'bluetooth';
interface PluginModule {
    default: (liff: any) => {} | Promise<{}>;
}
export interface Plugin<T = any> {
    checkSupport: (liff: T) => boolean;
    load: () => Promise<PluginModule> | PluginModule;
    errorMessage: string;
}
export declare type InitPlugins = (plugins: PluginName[]) => Promise<void[]>;
/**
 * Verify and load and initialize plugins.
 * @param   {PluginNames}  plugins - string array of plugin names
 * @returns {Promise<any>}
 */
export declare function createInitPlugins<T>(liff: T, verification?: {}): InitPlugins;
export {};
