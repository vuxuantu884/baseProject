declare class HandleLiffState {
    /**
     * get and validate Context
     * @returns {Context}
     */
    private getAndValidateContext;
    /**
     * Add slash on url string head
     * @returns {String}
     */
    private attachSlashAtStart;
    /**
     * Decode liff.state
     * If valid, return decoded state (with any double slashes removed)
     * Else LiffError is thrown
     * @param {string} state - liff.state's value
     * @throws {LiffError}
     * @returns {string} - Decoded liff.state with any double slashes removed
     */
    private decodeState;
    invoke(): Promise<void>;
}
export declare const handleLiffState: HandleLiffState;
export {};
