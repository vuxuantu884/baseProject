/**
 * pass liff id and feature_token to native-app with liff:// scheme
 * @export
 * @param {string} liffId
 * @param {string} featureToken
 * @returns {{Promise<void>}}
 */
export declare function sendLiffIdToClient(liffId: string, featureToken: string): void;
