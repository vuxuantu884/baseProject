import { Config } from '@liff/types';
import { ModuleDriver } from '@liff/use';
import { LiffInstance } from './types';
export declare function subsequentInit(liff: LiffInstance, moduleDriver: ModuleDriver, config: Config): Promise<void>;
