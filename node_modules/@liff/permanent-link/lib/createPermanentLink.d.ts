interface RequestParameters {
    liffId: string;
    currentPageUrl: string;
}
interface Response {
    permanentLinkUrl: string;
}
/**
 * Create Permanent Link
 * API Reference: please check document (LIFF V2 Restricted APIs for LIFF JS SDK)
 * @export
 * @returns {Promise<Response>}
 */
export default function createPermanentLink(postData: RequestParameters): Promise<Response>;
export {};
