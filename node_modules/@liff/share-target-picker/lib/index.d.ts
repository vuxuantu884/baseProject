import { SendMessagesParams } from '@liff/send-messages';
import { ShareTargetPickerResult } from './def';
import { LiffModule } from '@liff/use';
declare type ShareTargetPickerOptions = {
    isMultiple?: boolean;
};
declare type ShareTargetPickerMethod = (messages: SendMessagesParams, options?: ShareTargetPickerOptions) => Promise<ShareTargetPickerResult | void>;
export interface ExtendLiffCore {
    shareTargetPicker: ShareTargetPickerMethod;
}
export declare class ShareTargetPickerModule implements LiffModule<ShareTargetPickerMethod> {
    name: "shareTargetPicker";
    install(): ShareTargetPickerMethod;
    /**
     * Displays the target picker and sends the message created by the developer to the selected target.
     * This message appears to your group or friends as if you had sent it
     * @export
     * @param {string} routerMode
     * @returns {Promise<string|null>}
     */
    private shareTargetPicker;
    private checkPermission;
}
export declare const module: ShareTargetPickerModule;
export {};
